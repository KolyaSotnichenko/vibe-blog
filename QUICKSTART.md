## Quickstart

This repository currently contains task metadata and an OpenAPI specification.

When frontend code is added:
- Install dependencies with your package manager.
- Generate the OpenAPI client from `.vibe/openapi/openapi.json`.
- Use the generated client exclusively through a centralized API layer.

See `AGENT.md` for mandatory implementation rules.
